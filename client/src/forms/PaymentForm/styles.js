import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles((theme) => ({
  root: {},
  divider: {
    marginTop: theme.spacing(2),
    marginBottom: theme.spacing(2)
  },
  financialFundContainer: {
    marginBottom: theme.spacing(2)
  }
}))

export default useStyles
